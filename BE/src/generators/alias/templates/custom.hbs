import { FastifyRequest } from 'fastify';
import { {{serviceName}} } from '../../alias/{{serviceFile}}/{{serviceFile}}.service'

export class Custom{{serviceName}} extends {{serviceName}} {

  constructor() {
    super()
  }
}